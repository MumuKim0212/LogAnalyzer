# LogAnalyzer Agent Instructions

## Release
- **Do not commit binaries.** `/bin`, `/obj`, `.exe`, `.dll` are gitignored.
- To release, push a tag starting with `v` (e.g. `v1.0.0`). GitHub Actions will build and publish automatically.
